import 'package:flutter/material.dart';
import 'package:flutter_learning/screen/preferredSize.dart';

class myContainerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: Myappbar1(),
      backgroundColor: Colors.deepPurple,
      body: Column(
        children: [
          SizedBox(
            height: 20,
          ),
          Row(
            children: [
              SizedBox(width: 20),
              Container(
                  width: 120,
                  height: 120,
                  padding: EdgeInsets.all(5),
                  decoration: BoxDecoration(
                      shape: BoxShape.circle,
                      gradient:
                          LinearGradient(colors: [Colors.yellow, Colors.red]),
                      // color: Colors.black,
                      // border: Border(
                      //     bottom: BorderSide(color: Colors.white, width: 4),
                      //     top: BorderSide(color: Colors.white, width: 4)),
                      boxShadow: [
                        BoxShadow(
                            color: Colors.white.withOpacity(0.4),
                            blurRadius: 30,
                            spreadRadius: 3,
                            offset: Offset(2, 2))
                      ]),
                  child: Container(
                    decoration: BoxDecoration(
                      color: Colors.black,
                      shape: BoxShape.circle,
                    ),
                    child: Icon(Icons.person, size: 80, color: Colors.white),
                  )),
              Column(
                crossAxisAlignment: CrossAxisAlignment.start,
                children: [
                  SizedBox(width: 30),
                  Text("Username...........",
                      style: TextStyle(
                          fontSize: 25,
                          fontWeight: FontWeight.bold,
                          fontStyle: FontStyle.italic,
                          color: Colors.white,
                          decoration: TextDecoration.underline,
                          decorationColor: Colors.white,
                          decorationThickness: 2.0)),
                  SizedBox(width: 20),
                  Text("India..........",
                      style: TextStyle(
                          fontSize: 25,
                          fontWeight: FontWeight.bold,
                          fontStyle: FontStyle.italic,
                          color: Colors.white,
                          decoration: TextDecoration.underline,
                          decorationColor: Colors.white,
                          decorationThickness: 2.0)),
                ],
              ),
            ],
          ),
          SizedBox(height: 80),
          Container(
            padding: EdgeInsets.all(10),
            width: 400,
            height: 320,
            //color: Colors.black,
            decoration: BoxDecoration(
              borderRadius: BorderRadius.only(
                  topLeft: Radius.circular(20),
                  bottomRight: Radius.circular(20)),
              gradient: LinearGradient(colors: [Colors.yellow, Colors.red]),
              // border: Border(
              //     bottom: BorderSide(, width: 4),
              //     top: BorderSide(color: Colors.white, width: 4)),
              boxShadow: [
                BoxShadow(
                    color: Colors.white.withOpacity(0.4),
                    blurRadius: 30,
                    spreadRadius: 3,
                    offset: Offset(2, 2))
              ],
            ),
            child: Container(
              decoration: BoxDecoration(
                color: Colors.black,
                borderRadius: BorderRadius.circular(12),
              ),
              padding: EdgeInsets.all(16),
              child: Center(
                child: Column(
                  children: [
                    Text(
                      "→ Flutter enthusiast😊| Dart explorer🎯. Building beautiful cross-platform apps, one widget at a time🚀💙. Currently learning.",
                      style: TextStyle(fontSize: 20, color: Colors.white),
                      textAlign: TextAlign.justify,
                    ),
                    SizedBox(height: 50),
                    Container(
                      decoration: BoxDecoration(
                          gradient: LinearGradient(
                              colors: [Colors.yellow, Colors.red]),
                          borderRadius: BorderRadius.circular(20)),
                      padding: EdgeInsets.all(3),
                      child: ElevatedButton(
                        onPressed: () {},
                        style: ButtonStyle(
                          backgroundColor:
                              MaterialStateProperty.all(Color(0xffaa83ff)),
                          shape: MaterialStateProperty.all(
                            RoundedRectangleBorder(
                              borderRadius: BorderRadius.circular(28),
                            ),
                          ),
                        ),
                        child: Text("Follow me",
                            style: TextStyle(
                                fontSize: 25,
                                fontWeight: FontWeight.bold,
                                color: Colors.white)),
                      ),
                    ),
                  ],
                ),
              ),
            ),
          ),
        ],
      ),
    );
  }
}
